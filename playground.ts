// @ts-nocheck
import { ILaunchpad } from "launchpad.js"
import createGrid, { Grid } from "./src/launchpad/Grid"

let lp: ILaunchpad
const grid = createGrid(lp)
Grid.Panel.Right
console.log("DSDS")
// console.log(grid)